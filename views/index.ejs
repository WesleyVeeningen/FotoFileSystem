<!DOCTYPE html>
<html>

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>
    <%= title %>
  </title>
  <link rel='stylesheet' href='/stylesheets/output.css' />
  <script src="https://kit.fontawesome.com/f8991e1757.js" crossorigin="anonymous"></script>
</head>

<body class="flex w-screen bg-gray-100">

  <% include components/navbar.ejs %>

    <main class="ml-[15rem] h-screen w-full py-2 px-5">

      <div id="home">
        <h1 class="text-3xl font-bold">Welkom bij FotoFileSystem</h1>
        <p class="text-lg">Hier kunt u uw foto's opslaan en beheren.</p>
      </div>

      <div id="Files" class="hidden">
        <h1 class="text-3xl font-bold">Bestanden</h1>
        <p class="text-lg">Hier kunt u uw bestanden bekijken en downloaden.</p>
        <table id="recentFilesTable" class="table-auto w-full">
          <thead>
            <tr class="bg-gray-200">
              <th class="px-4 py-2">Bestandsnaam</th>
              <th class="px-4 py-2">Bestandstype</th>
              <th class="px-4 py-2">Grootte</th>
              <th class="px-4 py-2">Aangemaakt op</th>
            </tr>
          </thead>
          <tbody id="recentFilesBody">
            <!-- Render recent files directly in EJS template -->
            <% recentFiles.forEach(file=> { %>
              <tr>
                <td class="px-4 py-2">
                  <%= file.name %>
                </td>
                <td class="px-4 py-2">
                  <%= file.fileType %>
                </td>
                <td class="px-4 py-2">
                  <%= file.size %>
                </td>
                <td class="px-4 py-2">
                  <%= file.createdAt %>
                </td>
              </tr>
              <% }); %>
          </tbody>
        </table>
      </div>

    </main>

</body>

</html>