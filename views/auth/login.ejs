<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link rel='stylesheet' href='/stylesheets/output.css' />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css">
</head>

<body class="h-screen flex items-center justify-center bg-gray-100">

    <main class="container mx-auto flex justify-center">
        <div class="w-full max-w-md bg-white p-8 rounded-lg shadow-lg">
            <h1 class="text-3xl font-bold mb-6 text-center">Login</h1>
            <form action="/auth/login" method="POST" class="flex w-full flex-col">
                <div class="mb-4">
                    <label for="email" class="block mb-2">Email:</label>
                    <input type="email" id="email" name="email" class="form-input w-full"
                        value="<%= typeof email != 'undefined' ? email : '' %>" />
                    <% if (errors.email) { %>
                        <p class="text-red-500 text-sm">
                            <%= errors.email %>
                        </p>
                        <% } %>
                </div>
                <div class="mb-4">
                    <label for="password" class="block mb-2">Password:</label>
                    <input type="password" id="password" name="password" class="form-input w-full" />
                    <% if (errors.password) { %>
                        <p class="text-red-500 text-sm">
                            <%= errors.password %>
                        </p>
                        <% } %>
                </div>
                <div class="mb-4">
                    <button
                        class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded w-full">Login</button>
                </div>
            </form>
            <p class="text-center text-sm">Don't have an account? <a href="/auth/register"
                    class="text-blue-500">Register
                    here</a></p>
        </div>
    </main>

</body>

</html>